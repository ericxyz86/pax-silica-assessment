# Source migration — NOT READY TO DEPLOY

Staged from the Claude Design handoff bundle (31 Jul 2026). `site/index.html` is the
340 KB editable source; the 12 MB bundle at the repo root is still what is live.

## Done
- All 19 of the 31 July corrections re-applied to the source (DOE stakeholder split,
  §22.1 item 9, dating block + 7 downstream cutoff references, the four v2.6 additions,
  version strings to v1.3).

## Blocking: the handoff predates the v2.5 propagation
Claude Design's project never received the v2.5 substance edits or the /no-ai-slop pass,
because both were applied directly to the deployed bundle. 18 sentences present on the
live site are absent from this source. Do not cut over until they are ported.
